import 'package:flutter/cupertino.dart';

class ChatCareen extends StatelessWidget {
  const ChatCareen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("ChatScreen"),
    );
  }
}
